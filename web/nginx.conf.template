server {
    listen 80;
    server_name _;

    # API proxy to wukongim node1
    location /route {
        proxy_pass http://wk-node1:5001;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    }

    location /user/ {
        proxy_pass http://wk-node1:5001;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }

    location /channel/ {
        proxy_pass http://wk-node1:5001;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }

    location /message/ {
        proxy_pass http://wk-node1:5001;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }

    location /conversation/ {
        proxy_pass http://wk-node1:5001;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }

    location /streammessage/ {
        proxy_pass http://wk-node1:5001;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }

    location /conversations/ {
        proxy_pass http://wk-node1:5001;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }

    # Uploaded files (shared volume with wk-node1)
    location = /files {
        return 301 /files/;
    }

    location /files/ {
        alias /usr/share/nginx/uploads/;
    }

    # File upload API
    location /file/ {
        proxy_pass http://wk-node1:5001;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        client_max_body_size 50m;
    }

    # Admin manager API
    location /manager/ {
        proxy_pass ${WK_WEB_API_URL};
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    # Chat demo (exact path without trailing slash -> redirect)
    location = /chatdemo {
        return 301 /chatdemo/;
    }

    # Chat demo
    location /chatdemo/ {
        alias /usr/share/nginx/chatdemo/;
        try_files $uri $uri/ /chatdemo/index.html;
    }

    # Prometheus proxy
    location = /prometheus {
        return 301 /prometheus/;
    }

    location /prometheus/ {
        proxy_pass http://prometheus:9090/;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }

    # Admin panel
    location / {
        root /usr/share/nginx/html;
        index index.html;
        try_files $uri $uri/ /index.html;
    }
}
