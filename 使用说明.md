# 极速通 — 企业即时通讯平台使用说明

## 一、入门

### 1.1 获取账号

极速通采用**管理员后台统一分配账号**的方式，不支持自行注册。

- 联系管理员获取你的 **UID** 和 **初始密码**
- 首次登录后系统会要求修改密码

### 1.2 登录

1. 打开极速通网页端
2. 输入管理员分配的 **UID** 和 **密码**
3. 点击「登录」

![登录界面示意：输入 UID 和密码]

### 1.3 首次登录修改密码

如果管理员为你设置了初始密码，登录后会自动跳转到个人信息页面，你必须先修改密码才能正常使用。修改密码时**无需输入原密码**，直接设置新密码即可。

---

## 二、主界面概览

登录后进入主聊天界面，分为三个区域：

| 区域 | 位置 | 功能 |
|------|------|------|
| 会话列表 | 左侧栏 | 显示所有聊天会话，点击切换 |
| 聊天窗口 | 中间 | 发送和查看消息 |
| 文件/成员面板 | 右侧栏（可切换） | 群文件管理 / 群成员列表 |

顶部状态栏显示：
- **绿色圆点**：已连接
- **红色圆点**：连接断开，检查网络

---

## 三、消息功能

### 3.1 发送文字消息

在底部输入框输入文字，按 **Enter** 发送。

### 3.2 @提及（群聊）

在群聊中输入 `@` 或点击输入框左侧的 **@按钮**，可选择：
- **@所有人**：通知群内全体成员
- **@具体成员**：通知指定成员

### 3.3 回复消息

鼠标悬停在某条消息上，点击**回复按钮**，即可引用该消息进行回复。

### 3.4 发送文件/图片

点击输入框左侧的**回形针按钮**，选择文件发送。

支持的文件类型：
- 图片（jpg / png / gif / webp 等）
- 视频（mp4 / mov / avi 等）
- 文档（PDF / Word）
- 压缩包（zip）
- 文本文件（txt）

**发送图片的快捷方式**：直接 Ctrl+V 粘贴剪贴板中的图片。

### 3.5 取消上传

文件上传过程中，进度条右侧会出现**红色 × 按钮**，点击即可取消上传。选错文件时可以随时中止。

### 3.6 发送语音

按住输入框右侧的**麦克风按钮**开始录音，松开自动发送。

### 3.7 消息操作（右键）

右键点击自己发送的消息，可执行以下操作：

| 操作 | 说明 |
|------|------|
| 撤回 | 发送后 2 分钟内可撤回 |
| 置顶 | 将消息置顶到群聊（仅群主/管理员可操作） |

### 3.8 消息搜索

点击聊天窗口顶部的**搜索按钮**，输入关键词：

- 匹配的消息会高亮显示
- 点击「定位到聊天位置」可快速跳转
- 点击搜索框右侧 × 清除搜索

### 3.9 只看文件

点击聊天窗口顶部的**文件筛选按钮**，只显示图片和文件消息，方便浏览历史文件。

---

## 四、群聊管理

### 4.1 创建群聊

1. 点击会话列表上方的 **+** 按钮
2. 选择「创建群聊」
3. 输入**群名称**和你在群内的**昵称**
4. 点击创建

> 创建成功后系统会自动分配一个**房间号**，可在群信息面板中查看。

### 4.2 加入群聊

1. 点击 **+** →「加入群聊」
2. 输入群 ID 搜索
3. 确认群名称无误后，输入你的群昵称加入

### 4.3 查看群信息

点击聊天窗口右上角的**群名称**，打开群信息面板，可以查看：

- 群名称和房间号
- 成员列表（群主标记、在线状态）
- 我的群昵称

### 4.4 修改群昵称

在群信息面板中点击「我的群昵称」旁的编辑按钮，输入新昵称保存。

### 4.5 群设置（仅群主）

群主在群信息面板中可进行以下管理操作：

| 操作 | 说明 |
|------|------|
| 修改群名称 | 重命名群聊 |
| 踢出成员 | 移除指定成员 |
| 解散群聊 | 删除群组（不可恢复） |

### 4.6 退出群聊

非群主成员可在群信息面板底部点击「退出群聊」。

---

## 五、企业通讯录

### 5.1 查看成员

点击右侧面板的**「成员」标签**，按群组查看企业内所有成员。

每位成员显示：
- 头像（姓名首字）
- 用户名称
- UID
- 角色标记（群主 / 我）

### 5.2 展开/折叠

点击群组标题可展开或折叠该群组的成员列表。

---

## 六、群文件管理

### 6.1 浏览文件

点击右侧面板的**「文件」标签**，可浏览群内所有共享文件。

文件按类型自动分类：
- 全部文件
- 图片
- 视频
- 音频
- 文档

### 6.2 搜索文件

在文件面板顶部搜索框输入文件名或发送者，快速定位文件。

### 6.3 文件操作

| 操作 | 方式 |
|------|------|
| 预览 | 点击图片或 PDF 文件 |
| 下载 | 点击文件卡片 |
| 移动到分类 | 右键文件 → 选择分类 |
| 保存到我的文档 | 右键文件 → 保存到我的文档 |
| 定位到聊天 | 右键文件 → 跳转到对应消息 |

### 6.4 自定义分类

在文件面板中可以创建自定义文件夹，将文件整理归类。右键分类可重命名或删除（删除分类不会删除文件）。

---

## 七、个人中心

点击左上角头像区域进入个人信息页面。

### 7.1 查看信息

- **UID**：系统唯一标识（不可修改）
- **用户名称**：由管理员设定
- **部门**：所属部门
- **职位**：职位名称
- **角色**：权限角色（超级管理员 / 管理员 / 项目负责人 / 员工）

### 7.2 修改密码

1. 输入原密码（首次改密则无需输入）
2. 输入新密码（至少 6 位）
3. 确认新密码
4. 点击「修改密码」

---

## 八、消息置顶

在群聊中，群主可以将重要消息**置顶**，方便成员查看。

1. 右键点击要置顶的消息
2. 选择「置顶」
3. 置顶消息会显示在群信息面板中

取消置顶：右键已置顶消息 →「取消置顶」（仅群主或置顶者本人可操作）。

---

## 九、日历导航

点击聊天窗口顶部的**日历按钮**，可查看有消息记录的日期。

- 蓝色圆点标记表示当天有消息
- 点击日期直接跳转到对应位置
- 使用左右箭头切换月份

---

## 十、角色与权限

| 操作 | 超级管理员 | 管理员 | 项目负责人 | 员工 |
|------|:---:|:---:|:---:|:---:|
| 发送消息 | ✓ | ✓ | ✓ | ✓ |
| 发送文件/图片 | ✓ | ✓ | ✓ | ✓ |
| 查看企业通讯录 | ✓ | ✓ | ✓ | ✓ |
| 浏览群文件 | ✓ | ✓ | ✓ | ✓ |
| 创建群聊 | ✓ | ✓ | ✓ | ✗ |
| 管理自己的群 | ✓ | ✓ | ✓ | ✗ |
| 管理所有账号 | ✓ | ✓ | ✗ | ✗ |

---

## 十一、常见问题

**Q：忘记密码怎么办？**
联系管理员重置密码。

**Q：为什么不能创建群聊？**
普通员工没有创建群聊的权限，请联系项目负责人或管理员创建。

**Q：发送消息显示失败？**
检查网络连接状态（顶部圆点颜色），如果红色表示已断线，等待自动重连或刷新页面。

**Q：文件上传到一半可以取消吗？**
可以，点击上传进度条右侧的红色 × 按钮即可中止。

**Q：管理员离职后群怎么办？**
管理员处理离职时，系统会自动将该成员创建的群转让给群内其他成员。

---

## 十二、快捷键

| 快捷键 | 功能 |
|--------|------|
| Enter | 发送消息 |
| Ctrl + V | 粘贴图片 |

---

> 如有其他问题，请联系系统管理员。
